#include "caddieMenuOption.h"

using namespace nw4r;

namespace caddie {

const ut::Color IMenuOption::sEnabledTextColor(255, 255, 255, 255);
const ut::Color IMenuOption::sDisabledTextColor(142, 142, 142, 255);
const ut::Color IMenuOption::sEnabledStrokeColor(0, 0, 0, 255);
const ut::Color IMenuOption::sDisabledStrokeColor(0, 0, 0, 255);

} // namespace caddie
